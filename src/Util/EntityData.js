class EntityData {
  constructor(id, title, value, type, options) {
    this.id = id;
    this.title = title;
    this.value = value;
    this.type = type;
    this.options = options;
  }
}

export default EntityData;
